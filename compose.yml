services:
  app:
    build:
      context: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    command: >
      sh -c "
      apt update && 
      cd app &&
      npm update &&
      pip install 
      npm run dev
      "
volumes:
  node_modules: